spring:
  datasource:
    url: jdbc:h2:mem:dbiot
    username: sa
    schema: classpath:schema.sql
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true